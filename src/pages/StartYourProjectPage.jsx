import { EnquiryFormSection } from '../components/sections/EnquiryFormSection.jsx'

/** WP `page-start-your-project.php` — enquiry form only (no hero). */
export function StartYourProjectPage() {
  return <EnquiryFormSection />
}
